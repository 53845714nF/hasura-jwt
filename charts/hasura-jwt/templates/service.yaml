apiVersion: v1
kind: Service
metadata:
  name: {{ include "hasura-jwt.fullname" . }}-service
  labels:
    {{- include "hasura-jwt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hasura-jwt.selectorLabels" . | nindent 4 }}
